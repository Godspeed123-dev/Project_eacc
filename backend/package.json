{
  "name": "eacc-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express server for the E/ACC site — serves the static frontend and proxies/caches the price API.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
